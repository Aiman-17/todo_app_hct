# Backend Environment Variables
# Copy this file to .env and fill in actual values

# Database Configuration
# Get connection string from Neon dashboard: https://neon.tech
DATABASE_URL=postgresql://[user]:[password]@[endpoint]/[database]?sslmode=require

# Authentication Secret
# Generate with: openssl rand -hex 32 (Linux/macOS) or
# [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 })) (Windows PowerShell)
# MUST be minimum 32 characters
# MUST match frontend BETTER_AUTH_SECRET
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long-change-this-in-production

# CORS Configuration
# Comma-separated origins allowed to access the API
# Development: http://localhost:3000
# Production: https://your-domain.com
CORS_ORIGINS=http://localhost:3000

# Phase III: AI Chatbot Configuration
# Google Gemini API Key for natural language processing (Free Tier: 1M tokens/month)
# Get from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# MCP Tools Logging Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
MCP_TOOLS_LOG_LEVEL=INFO
